<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to Rails.application.class.parent_name.downcase, static_pages_about_path, class: 'navbar-brand' %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <!-- <li class="<%#= 'active' if current_page?(root_path) %>"><%#= link_to "Home", root_path %></li> -->
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle profile-picture-a" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              <% if current_user.profile_picture %>
                <%= image_tag current_user.profile_picture, class: 'img-circle' %>
              <% else %>
                <span class="glyphicon glyphicon-user" aria-hidden="true"></span>
              <% end %>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t('custom.navbar.edit'), edit_user_registration_path %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>  
        <% else %>
          <li class="<%= 'active' if current_page?(new_user_registration_path) %>">
            <%= link_to t('devise.shared.links.sign_up'), new_user_registration_path %>
          </li>
          <li class="<%= 'active' if current_page?(new_user_session_path) %>">
            <%= link_to t('devise.shared.links.sign_in'), new_user_session_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>